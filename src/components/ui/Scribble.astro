<svg
    class="scribble"
    xmlns="http://www.w3.org/2000/svg"
    fill="none"
    viewBox="0 0 720 21"
>
    <path
        stroke="currentColor"
        stroke-width="3"
        d="M3.506 12.69c-34 0 519-23 374 0s358.5-31.5 340 0"></path>
</svg>

<style>
    .scribble {
        color: var(--c-red);
    }
</style>

<script>
    import gsap from "gsap";
    import { DrawSVGPlugin } from "gsap/DrawSVGPlugin";

    gsap.registerPlugin(DrawSVGPlugin);

    document.fonts.ready.then(() => {
        gsap.from(".scribble path", {
            drawSVG: "0%",
            duration: 2,
            ease: "power1.out",
        });
    });
</script>
